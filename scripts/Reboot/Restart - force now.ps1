# Force a restart in 5 seconds. Logged-on users are NOT prompted and lose unsaved work.
# The short timer lets this line return before the session drops.
shutdown.exe /r /t 5 /f /c "Vivre: restarting now."
'Restart scheduled in 5 seconds (forced).'
