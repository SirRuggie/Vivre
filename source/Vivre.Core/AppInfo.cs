namespace Vivre.Core;

/// <summary>
/// Static metadata about the application. <c>Vivre</c> — French <em>"to live"</em>; named for
/// the One Piece Vivre Card, a slip that tracks a person's life force (where they are, whether
/// they're hurt, how they're holding up). Every row in the grid is a Vivre Card for one machine.
/// </summary>
public static class AppInfo
{
    public const string ProductName = "Vivre";

    public const string Tagline = "The heartbeat of your fleet.";

    public const string Edition = ".NET 10 / WPF";
}
