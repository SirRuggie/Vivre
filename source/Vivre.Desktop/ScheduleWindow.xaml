<ui:FluentWindow x:Class="Vivre.Desktop.ScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Vivre.Desktop"
        mc:Ignorable="d"
        Title="Schedule install"
        Height="280" Width="440"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Schedule install" />

        <StackPanel Grid.Row="1" Margin="24,12,24,0">
            <ui:TextBlock Appearance="Secondary" Margin="0,0,0,16" TextWrapping="Wrap"
                          Text="Pick when the install should run on the selected machine(s). A one-time task runs as SYSTEM at that time; reboot is reported, not forced." />

            <StackPanel Orientation="Horizontal">
                <ui:TextBlock Text="Date" Width="50" VerticalAlignment="Center" />
                <DatePicker x:Name="DatePick" Width="170" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <ui:TextBlock Text="Time" Width="50" VerticalAlignment="Center" />
                <ComboBox x:Name="HourBox" Width="70" />
                <ui:TextBlock Text=":" Margin="8,0" VerticalAlignment="Center" FontWeight="Bold" />
                <ComboBox x:Name="MinuteBox" Width="70" />
                <ui:TextBlock Text="(24-hour)" Margin="12,0,0,0" VerticalAlignment="Center" Appearance="Secondary" />
            </StackPanel>

            <ui:TextBlock x:Name="Hint" Margin="0,12,0,0" Foreground="{DynamicResource SystemFillColorCautionBrush}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,16">
            <ui:Button Appearance="Primary" Content="Schedule" Click="OnSchedule" IsDefault="True" />
            <ui:Button Margin="8,0,0,0" Content="Cancel" Click="OnCancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
