<ui:FluentWindow x:Class="Vivre.Desktop.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Vivre.Desktop"
        mc:Ignorable="d"
        Title="Settings"
        SizeToContent="Height" Width="520" MinWidth="480"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <ui:FluentWindow.Resources>
        <local:InverseBooleanConverter x:Key="InverseBoolConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Settings" />

        <StackPanel Grid.Row="1" Margin="24,12,24,0">
            <ui:TextBlock FontTypography="BodyStrong" Text="Remote credentials" />
            <ui:TextBlock Appearance="Secondary"
                          Margin="0,2,0,12"
                          TextWrapping="Wrap"
                          Text="Used for SCCM actions, Run Script, and Enable WinRM. Held for this session only — never saved to disk." />

            <RadioButton x:Name="UseLoginRadio"
                         Content="Use my Windows login"
                         IsChecked="{Binding UseExplicitCredentials, Converter={StaticResource InverseBoolConverter}}"
                         Margin="0,0,0,6" />
            <RadioButton x:Name="UseExplicitRadio"
                         Content="Use these credentials"
                         IsChecked="{Binding UseExplicitCredentials}" />

            <Grid Margin="24,10,0,0" IsEnabled="{Binding UseExplicitCredentials}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ui:TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" Text="Domain" />
                <ui:TextBox   Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                              PlaceholderText="e.g. EMPLOYEES"
                              Text="{Binding Domain, UpdateSourceTrigger=PropertyChanged}" />

                <ui:TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" Text="Username" />
                <ui:TextBox   Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" x:Name="UserNameBox"
                              PlaceholderText="e.g. admin_sbridges"
                              Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />

                <ui:TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Password" />
                <PasswordBox  Grid.Row="2" Grid.Column="1" x:Name="PasswordBox" Height="32" VerticalContentAlignment="Center" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,0,24,16">
            <ui:Button Appearance="Primary" Content="Save" Click="OnSave" IsDefault="True" />
            <ui:Button Margin="8,0,0,0" Content="Cancel" Click="OnCancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
